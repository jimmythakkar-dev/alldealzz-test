<div style="float:left; margin-left:5%;"class="dashboard-forms login-form">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { role: "form" }) do |f| %>
    <div class="form-group">
      <%= f.label 'Email / Username' %>
      <%= f.text_field :login, autofocus: true, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :password %>
      <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
    </div>
    <% if devise_mapping.rememberable? %>
      <div class="checkbox">
        <label>
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </label>
      </div>
      <% end %>
    <div class="p-container">
      <%#= link_to "REGISTER MALL", new_mall_path, class: "btn btn-primary submit", style: "float: left;" %>
      <%= f.submit  t('.sign_in', :default => "SIGN IN"), class: "btn btn-primary submit" %> <br />
    </div>
  <% end %>
</div>

<!--<div style="float:right; margin-right:5%;"class="dashboard-forms login-form">-->
	<%#= form_for(resource, as: resource_name, url: session_path(resource_name), html: { role: "form" }) do |f| %>
    <!--<div class="form-group">-->
      <%#= f.label 'Email / Username' %>
      <%#= f.text_field :login, autofocus: true, class: "form-control" %>
    <!--</div>-->
    <!--<div class="form-group">-->
      <%#= f.label :password %>
      <%#= f.password_field :password, autocomplete: "off", class: "form-control" %>
    <!--</div>-->
    <%# if devise_mapping.rememberable? %>
      <!--<div class="checkbox">-->
        <!--<label>-->
          <%#= f.check_box :remember_me %>
          <%#= f.label :remember_me %>
        <!--</label>-->
      <!--</div>-->
      <%# end %>
    <!--<div class="p-container"> -->
      <%#= link_to "REGISTER STORE", new_store_path, class: "btn btn-primary submit", style: "float: left;" %>
      <%#= f.submit  t('.sign_in', :default => "SIGN IN"), class: "btn btn-primary submit" %> <br />
    <!--</div>  -->
  <%# end %>
<!--</div>-->











<% if false %>
<div class="row">
  <div class="col-md-4"></div>
  <div class="col-md-4">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h4><%= t('.sign_in', :default => "Sign in") %></h4>
      </div>
      <div class="panel-body">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { role: "form" }) do |f| %>

          <div class="form-group">
            <%= f.label 'Email / Username' %>
            <%= f.text_field :login, autofocus: true, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :password %>
            <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
          </div>
          <% if devise_mapping.rememberable? %>
            <div class="checkbox">
              <label>
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              </label>
            </div>
            <% end %>
          <%= f.submit  t('.sign_in', :default => "Sign in"), class: "btn btn-primary" %> <br />
          <%= link_to "Register new store", new_store_path %> or
          <%= link_to "Register new mall", new_mall_path %>
        <% end %>
      </div>

    </div>
    <%= render "devise/shared/links" %>
  </div>
</div>
<% end %>  
